# Base URL of backend/api (SPEC.md §7) — not yet built (BUILD-PLAN.md feature #3).
# Port is not decided anywhere in SPEC.md yet; 4000 is a placeholder until the API spec fixes one.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
